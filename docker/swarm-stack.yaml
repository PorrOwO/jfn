networks:
  jfn_overlay:
    driver: overlay
    attachable: true
    driver_otps:
      com.docker.network.driver.mtu: 1250

services:
  etcd:
    image: quay.io/coreos/etcd:v3.5.9
    environment:
      - ETCD_ADVERTISE_CLIENT_URLS=http://0.0.0.0:2379
      - ETCD_LISTEN_CLIENT_URLS=http://0.0.0.0:2379
    ports:
      - target: 2379
        published: 2379
        protocol: tcp
        mode: host
    networks:
      - jfn_overlay
    deploy:
      replicas: 1
      placement:
        constraints: [node.role == manager]

  node-bootstrapper:
    image: docker:25-cli
    enviroment:
      - MANAGER_IP=${MANAGER_IP}
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - /home/porrowo/projects/jfn/docker/docker-compose.node.yml:/root/jfn/docker/docker-compose.node.yml
      - /home/porrowo/projects/jfn:/root/jfn
    working_dir: /root/jfn/docker
    command: >
      sh -c "docker network create jfn 2>/dev/null || true;
             docker compose -f docker-compose.node.yml down --remove-orphans 2>/dev/null;
             docker compose -f docker-compose.node.yml up -d --remove-orphans && tail -f /dev/null"
    networks:
      - jfn_overlay
    deploy:
      mode: global
      placement:
        constraints: [node.labels.jfn-role == worker]
      restart_policy:
        condition: on-failure

  nginx:
    image: nginx:alpine
    ports:
      - target: 8000
        published: 8000
        protocol: tcp
        mode: host
    volumes:
      - /home/porrowo/projects/jfn/docker/nginx.conf:/etc/nginx/nginx.conf:ro
    networks:
      - jfn_overlay
    deploy:
      replicas: 1
      placement:
        constraints: [node.role == manager]